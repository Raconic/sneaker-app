<%- include('../partials/header') %>
<h1>Sneakers</h1>
    <ul>
        <% sneakers.forEach(function(s) { %>
            <li>
                <a href="/sneakers/<%= s.id %>">
                    <%= s.brandName %>
                    <%= s.modelName %>
                
                </a>

            </li>
            <% }); %>
    </ul>

<%- include('../partials/footer') %>