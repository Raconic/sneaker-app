<%- include('../partials/header') %>
<h1>collections</h1>
    <ul>
        <% collections.forEach(function(s) { %>
            <li>
                <a href="/collections/<%= c.id %>">    
                </a>

            </li>
            <% }); %>
    </ul>

<%- include('../partials/footer') %>